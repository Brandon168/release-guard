# Low-Risk Example

Change:
Update deck copy in `presentation/slides.md` and tweak hero text styling in the
demo without changing any API route, risk policy, or deterministic logic.

Why it calibrates as low:

- isolated to presentation content
- easy to revert
- no CI blocking logic or runtime endpoint behavior changes
